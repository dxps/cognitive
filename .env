##
## DATABASE_URL is used by:
## 1. `sqlx::query!` macros that do compile time checks to validate the queries.
## 2. `sqlx migrate run` command.
## 3. The server side, to connect to the database.
##

DATABASE_URL="postgres://servicentral:servicentral@localhost:5443/servicentral"

## The logo is optional. If specified, it points to an image that must be found in assets folder.
LOGO_PATH="/logo.png"
